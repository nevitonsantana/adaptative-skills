# Efficiency Layer Examples

This folder will hold short examples for Efficiency Layer patterns.

The first implementation pass is documentation-first, so examples are intentionally light.

## Planned early examples

- bounded execution with `task-chunking`
- round closure with `handoff-summary`
- stop-or-continue decision with `checkpoint-review`
